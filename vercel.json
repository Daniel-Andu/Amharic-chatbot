{
  "version": 2,
  "builds": [
    {
      "src": "customer-chat/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build",
        "buildCommand": "cd customer-chat && CI=true GENERATE_SOURCEMAP=false npm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/customer-chat/index.html"
    }
  ]
}